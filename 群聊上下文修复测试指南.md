# 群聊上下文修复测试指南

## 修复内容

### 问题
群聊模式下，每个角色都只看到用户的原始问题，而不是看到完整的对话历史（包括其他角色的回答）。

### 根本原因
每个角色的请求都会保存一次用户消息到数据库，导致：
1. 数据库中有多条相同的用户消息（但appId不同）
2. 历史查询时可能出现混乱

### 解决方案
在群聊模式下，只保存一次用户消息：
- 第一个角色请求时：保存新的用户消息
- 后续角色请求时：复用已存在的用户消息（10秒时间窗口内）

## 修改的文件

### 1. service/src/modules/chatLog/chatLog.service.ts
添加了新方法 `findRecentUserLogInGroup`：
```typescript
/* 查询群组中最近的用户消息（用于群聊模式避免重复保存） */
async findRecentUserLogInGroup(groupId: number, prompt: string, timeWindowSeconds: number = 10): Promise<any> {
  // 查询最近N秒内的用户消息
  const timeThreshold = new Date(Date.now() - timeWindowSeconds * 1000);
  
  const userLog = await this.chatLogEntity.findOne({
    where: {
      groupId: groupId,
      role: 'user',
      content: prompt,
      isDelete: false,
      createdAt: MoreThan(timeThreshold) as any,
    },
    order: {
      createdAt: 'DESC',
    },
  });

  return userLog;
}
```

### 2. service/src/modules/chat/chat.service.ts
修改了用户消息保存逻辑（第506-564行）：
```typescript
// 群聊模式下，检查是否已经保存过用户消息（避免重复保存）
let userSaveLog;
let userLogId;

if (isGroupChat && groupId) {
  // 查询最近10秒内是否有相同的用户消息
  const existingUserLog = await this.chatLogService.findRecentUserLogInGroup(groupId, prompt, 10);
  
  if (existingUserLog) {
    // 使用已存在的用户消息
    userLogId = existingUserLog.id;
    userSaveLog = existingUserLog;
    Logger.debug(`[群聊] 使用已存在的用户消息，id=${userLogId}, appId=${appId}`, 'ChatService');
  } else {
    // 第一次保存用户消息
    userSaveLog = await this.chatLogService.saveChatLog({...});
    userLogId = userSaveLog.id;
    Logger.debug(`[群聊] 保存新的用户消息，id=${userLogId}, appId=${appId}`, 'ChatService');
  }
} else {
  // 普通模式，正常保存
  userSaveLog = await this.chatLogService.saveChatLog({...});
  userLogId = userSaveLog.id;
}
```

添加了详细的日志输出（第953-967行和第1069-1127行）：
- 查询历史对话时的日志
- 构建消息历史时的日志
- 每条消息的详细信息

## 测试步骤

### 准备工作
1. 启动后端服务：
   ```bash
   cd service
   pnpm dev
   ```

2. 启动前端服务：
   ```bash
   cd chat
   pnpm dev
   ```

3. 打开浏览器控制台和后端日志窗口

### 测试场景1：基本群聊功能

1. **创建群聊**
   - 登录系统
   - 创建一个新的群聊对话组
   - 添加两个角色：
     - 角色A：名称"小明"，设置为活泼的性格
     - 角色B：名称"小红"，设置为温柔的性格

2. **发送第一条消息**
   - 在群聊中输入："你好，大家好！"
   - 点击发送

3. **观察后端日志**
   应该看到类似的日志输出：
   ```
   [群聊] 开始查询历史对话，groupId=xxx, maxRounds=10, appId=角色A的ID
   [群聊] 查询到历史记录数=0
   [群聊] 保存新的用户消息，id=xxx, appId=角色A的ID
   
   [群聊] 开始查询历史对话，groupId=xxx, maxRounds=10, appId=角色B的ID
   [群聊] 查询到历史记录数=2  // 用户消息 + 角色A的回复
   [群聊历史] 记录0: role=user, appId=角色A的ID, content=你好，大家好！
   [群聊历史] 记录1: role=assistant, appId=角色A的ID, content=小明的回复...
   [群聊] 使用已存在的用户消息，id=xxx, appId=角色B的ID
   [群聊历史构建] 当前角色appId=角色B的ID, 群组成员数=2
   [群聊历史构建] 用户消息数=1, AI消息数=1
   [群聊历史] 用户消息: 你好，大家好！
   [群聊历史] AI消息 (appId=角色A的ID, 当前=角色B的ID, role=user): 小明：小明的回复...
   ```

4. **观察前端显示**
   - 应该看到用户消息："你好，大家好！"
   - 然后看到角色A（小明）的回复
   - 最后看到角色B（小红）的回复，内容应该**考虑到了角色A的回复**

### 测试场景2：连续对话

1. **发送第二条消息**
   - 输入："你们觉得今天天气怎么样？"
   - 点击发送

2. **观察后端日志**
   - 角色A的请求应该看到：用户的第一条消息 + 角色A的回复 + 角色B的回复 + 用户的第二条消息
   - 角色B的请求应该看到：完整的对话历史，包括角色A对第二条消息的回复

3. **验证对话连贯性**
   - 角色B的回复应该能够引用角色A的观点
   - 例如：
     - 角色A："今天天气真好，阳光明媚！"
     - 角色B："我同意小明的看法，确实是个好天气，适合出去玩。"

### 测试场景3：数据库验证

1. **查询数据库**
   ```sql
   SELECT id, role, appId, content, createdAt 
   FROM chatlog 
   WHERE groupId = [你的群组ID] 
   ORDER BY createdAt ASC;
   ```

2. **验证结果**
   应该看到类似的记录：
   ```
   | id | role      | appId    | content              | createdAt           |
   |----|-----------|----------|----------------------|---------------------|
   | 1  | user      | 角色A的ID | 你好，大家好！        | 2025-01-01 10:00:00 |
   | 2  | assistant | 角色A的ID | 小明的回复...         | 2025-01-01 10:00:01 |
   | 3  | assistant | 角色B的ID | 小红的回复...         | 2025-01-01 10:00:02 |
   | 4  | user      | 角色A的ID | 你们觉得今天天气...   | 2025-01-01 10:00:10 |
   | 5  | assistant | 角色A的ID | 今天天气真好...       | 2025-01-01 10:00:11 |
   | 6  | assistant | 角色B的ID | 我同意小明的看法...   | 2025-01-01 10:00:12 |
   ```

   **关键验证点**：
   - 每条用户消息只出现一次（不会有重复的用户消息）
   - 用户消息的 `appId` 是第一个角色的ID
   - AI角色的回复都有正确的 `appId`

### 测试场景4：时间窗口测试

1. **发送消息后等待15秒**
   - 发送一条消息："测试时间窗口"
   - 等待15秒（超过10秒的时间窗口）

2. **手动触发第二个角色的请求**
   - 如果可能，手动调用API让第二个角色回复

3. **观察日志**
   - 应该看到"保存新的用户消息"而不是"使用已存在的用户消息"
   - 这是因为超过了10秒的时间窗口

## 预期结果

### 成功标准
1. ✅ 每条用户消息在数据库中只保存一次
2. ✅ 后续角色能够看到前面角色的回复
3. ✅ 角色之间的对话具有连贯性和上下文感知
4. ✅ 后端日志清晰显示消息构建过程
5. ✅ 前端显示正确，没有重复或缺失的消息

### 失败情况
如果出现以下情况，说明修复失败：
1. ❌ 数据库中有多条相同的用户消息
2. ❌ 角色B的回复没有考虑角色A的回复
3. ❌ 后端日志显示"查询到历史记录数=0"（对于第二个角色）
4. ❌ 前端显示的对话不连贯

## 调试技巧

### 1. 启用详细日志
后端已经添加了详细的日志输出，确保日志级别设置为 `debug`：
```typescript
// service/src/main.ts
app.useLogger(['log', 'error', 'warn', 'debug']);
```

### 2. 检查消息历史构建
在 `chat.service.ts` 的 `buildMessageFromParentMessageId` 方法中，添加断点或日志：
```typescript
Logger.debug(`[群聊历史构建] 最终消息数组: ${JSON.stringify(messages, null, 2)}`, 'ChatService');
```

### 3. 检查数据库查询
在 `chatLog.service.ts` 的 `chatHistory` 方法中，添加日志：
```typescript
Logger.debug(`[查询历史] SQL结果: ${JSON.stringify(list, null, 2)}`, 'ChatLogService');
```

### 4. 前端调试
在浏览器控制台中，检查请求和响应：
```javascript
// 查看发送的请求
console.log('[群聊请求]', requestData);

// 查看收到的响应
console.log('[群聊响应]', responseData);
```

## 回滚方案

如果修复导致问题，可以回滚到之前的版本：

### 1. 回滚 chatLog.service.ts
删除 `findRecentUserLogInGroup` 方法

### 2. 回滚 chat.service.ts
将用户消息保存逻辑改回：
```typescript
const userSaveLog = await this.chatLogService.saveChatLog({
  appId: appId,
  curIp,
  userId: req.user.id,
  type: modelType ? modelType : 1,
  fileUrl: fileUrl ? fileUrl : null,
  imageUrl: imageUrl ? imageUrl : null,
  content: prompt,
  promptTokens: 0,
  completionTokens: 0,
  totalTokens: 0,
  model: useModel,
  modelName: userName,
  role: 'user',
  groupId: groupId ? groupId : null,
});
const userLogId = userSaveLog.id;
```

## 后续优化建议

1. **添加单元测试**
   - 测试 `findRecentUserLogInGroup` 方法
   - 测试群聊消息历史构建逻辑

2. **性能优化**
   - 考虑使用缓存来存储最近的用户消息
   - 减少数据库查询次数

3. **用户体验优化**
   - 添加加载动画，显示"角色正在思考..."
   - 显示每个角色的发言顺序

4. **错误处理**
   - 添加更完善的错误处理机制
   - 当某个角色回复失败时，不影响其他角色

## 联系方式

如果在测试过程中遇到问题，请：
1. 检查后端日志，查找错误信息
2. 检查数据库，验证数据是否正确保存
3. 提供详细的复现步骤和日志输出

