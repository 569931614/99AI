# 群聊上下文问题修复说明

## 问题描述

当前群聊实现存在问题：每个角色都只看到用户的原始问题，而不是看到完整的对话历史（包括其他角色的回答）。

### 当前错误的行为
- 用户问："你好"
- 角色1看到：用户问题（"你好"）
- 角色2看到：用户问题（"你好"）← **错误：没有看到角色1的回答**

### 期望的正确行为（参考星尘API文档）
- 用户问："你好"
- 角色1看到：用户问题（"你好"）
- 角色2看到：用户问题（"你好"）+ 角色1的回答（作为user消息）

## 星尘API文档规范

根据阿里云星尘群聊API文档（https://help.aliyun.com/document_detail/2861866.html），群聊模式下的消息格式应该是：

### 消息角色规则
1. **真实用户的消息**：`role: 'user'`，不需要添加说话人前缀
2. **AI角色的消息**：
   - 如果是**当前角色自己**的历史消息：`role: 'assistant'`
   - 如果是**其他角色**的消息：`role: 'user'`，需要添加说话人前缀（如"角色A：内容"）

### 示例消息流

假设有两个角色：角色A和角色B

#### 角色A的请求（第一个发言）
```json
{
  "messages": [
    {"role": "system", "content": "你是角色A..."},
    {"role": "user", "content": "你好"}  // 用户的问题
  ]
}
```

#### 角色B的请求（第二个发言）
```json
{
  "messages": [
    {"role": "system", "content": "你是角色B..."},
    {"role": "user", "content": "你好"},  // 用户的问题
    {"role": "user", "content": "角色A：你好！很高兴见到你。"}  // 角色A的回答，作为user消息
  ]
}
```

## 当前实现分析

### 后端实现（service/src/modules/chat/chat.service.ts）

后端的消息历史构建逻辑（第1069-1127行）已经正确实现了星尘API规范：

```typescript
// 群聊模式：按照星尘文档要求构建消息
if (groupMembers.length > 0) {
  // 根据appId找到当前角色
  const currentMember = groupMembers.find(m => m.appId === appId || m.userId === appId);
  const currentAppId = currentMember ? (currentMember.appId || currentMember.userId) : appId;

  // 构建历史消息，按照星尘文档格式
  const allMessages = [...userMessages, ...assistantMessages].sort(
    (a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime(),
  );

  for (const msg of allMessages) {
    if (msg.role === 'user') {
      // 真实用户消息：role为user
      messages.push({
        role: 'user',
        content: msg.content
      });
    } else if (msg.role === 'assistant') {
      // AI角色消息：根据appId判断是否为当前角色
      const msgAppId = (msg as any).appId;
      const isCurrentMember = msgAppId && msgAppId === currentAppId;
      const finalRole = isCurrentMember ? 'assistant' : 'user';

      let messageContent = msg.content;

      // 如果是其他角色的回复，添加说话人标识
      if (!isCurrentMember && typeof messageContent === 'string') {
        const hasSpeakerPrefix = /^[^：]+：/.test(messageContent);
        if (!hasSpeakerPrefix) {
          const msgMember = groupMembers.find(m => (m.appId || m.userId) === msgAppId);
          const speakerName = msgMember ? (msgMember.appName || msgMember.name || `角色${msgAppId}`) : `角色${msgAppId}`;
          messageContent = `${speakerName}：${messageContent}`;
        }
      }

      messages.push({
        role: finalRole,
        content: messageContent
      });
    }
  }
}
```

**这个逻辑是正确的！** 它会：
1. 查询数据库中的历史消息
2. 将其他角色的消息转换为 `role: 'user'` 并添加说话人前缀
3. 将当前角色自己的消息保持为 `role: 'assistant'`

### 前端实现（chat/src/views/chat/chatBase.vue）

前端的群聊处理逻辑（第556-617行）：

```typescript
const handleGroupChatResponse = async () => {
  // 获取群聊成员信息
  const groupMembers = await getGroupMembers()
  
  // 连续请求每个角色发言
  for (let i = 0; i < groupMembers.length; i++) {
    const member = groupMembers[i]
    
    // 为每个角色创建独立的聊天记录
    const memberChat = { ... }
    addGroupChat(memberChat)
    
    // 发送请求让当前角色发言
    await sendGroupMemberRequest(member, i)
    
    // 等待当前角色回复完成
    await waitForMemberResponse(i)
    
    // 短暂延迟
    if (i < groupMembers.length - 1) {
      await new Promise(resolve => setTimeout(resolve, 300))
    }
  }
}
```

**这个逻辑也是正确的！** 它会：
1. 依次请求每个角色
2. 等待前一个角色回复完成后再请求下一个
3. 每个角色的请求都会传递相同的 `groupId` 和不同的 `appId`

## 问题根源

经过分析，问题可能出在以下几个地方：

### 1. 消息保存时机

虽然后端使用了 `await` 来保存消息，但是前端在收到响应后立即请求下一个角色，可能存在时序问题：

```typescript
// 前端等待300ms
await new Promise(resolve => setTimeout(resolve, 300))
```

这个延迟可能不够，导致下一个角色请求时，前一个角色的消息还没有完全保存到数据库。

### 2. 用户消息的重复保存

每个角色的请求都会保存一次用户消息（第511-526行）：

```typescript
const userSaveLog = await this.chatLogService.saveChatLog({
  appId: appId,
  userId: req.user.id,
  content: prompt,
  role: 'user',
  groupId: groupId ? groupId : null,
});
```

这意味着：
- 角色1请求时，保存用户消息（appId=角色1）
- 角色2请求时，又保存一次用户消息（appId=角色2）

这会导致数据库中有多条相同的用户消息，但是 `appId` 不同。

### 3. 历史查询逻辑

`chatHistory` 方法（chatLog.service.ts 第280-337行）查询历史时：

```typescript
const list = await this.chatLogEntity.find({
  where: { isDelete: false, groupId: groupId },
  order: { createdAt: 'DESC' },
  take: rounds * 2,
});
```

这个查询会返回所有 `groupId` 匹配的消息，包括：
- 用户消息（可能有多条，每个角色请求时都保存了一次）
- AI角色的回复

## 解决方案

### 方案1：只保存一次用户消息（推荐）

修改后端逻辑，在群聊模式下，只有第一个角色请求时才保存用户消息：

```typescript
// 群聊模式下，检查是否已经保存过用户消息
let userLogId = null;
if (isGroupChat) {
  // 查询是否已经有相同的用户消息
  const existingUserLog = await this.chatLogService.findUserLogInGroup(groupId, prompt);
  if (existingUserLog) {
    userLogId = existingUserLog.id;
    Logger.debug(`[群聊] 使用已存在的用户消息，id=${userLogId}`, 'ChatService');
  } else {
    // 第一次保存用户消息
    const userSaveLog = await this.chatLogService.saveChatLog({...});
    userLogId = userSaveLog.id;
    Logger.debug(`[群聊] 保存新的用户消息，id=${userLogId}`, 'ChatService');
  }
} else {
  // 普通模式，正常保存
  const userSaveLog = await this.chatLogService.saveChatLog({...});
  userLogId = userSaveLog.id;
}
```

### 方案2：增加前端延迟（临时方案）

增加前端的等待时间，确保消息完全保存到数据库：

```typescript
// 等待当前角色回复完成
await waitForMemberResponse(i)

// 增加延迟，确保消息保存到数据库
if (i < groupMembers.length - 1) {
  await new Promise(resolve => setTimeout(resolve, 1000)) // 从300ms增加到1000ms
}
```

### 方案3：添加消息保存确认机制（最佳方案）

修改后端，在消息保存完成后返回确认信号：

```typescript
// 后端返回响应时，包含消息ID
response.chatId = assistantLogId;
response.userChatId = userLogId;
response.saved = true; // 添加保存确认标志
```

前端等待保存确认：

```typescript
// 等待消息保存完成
const waitForMessageSaved = async (memberIndex: number) => {
  return new Promise(resolve => {
    const checkInterval = setInterval(() => {
      const chat = dataSources.value.find(c => c.memberIndex === memberIndex)
      if (chat && chat.chatId && !chat.loading) {
        clearInterval(checkInterval)
        resolve(true)
      }
    }, 100)
    
    setTimeout(() => {
      clearInterval(checkInterval)
      resolve(true)
    }, 30000)
  })
}
```

## 测试步骤

1. 创建一个群聊，包含两个角色：角色A和角色B
2. 发送消息："你好"
3. 观察后端日志，检查：
   - 角色A的请求：应该只看到用户消息
   - 角色B的请求：应该看到用户消息 + 角色A的回复（作为user消息）
4. 检查数据库，确认：
   - 用户消息只保存了一次（或者每个角色都保存了，但查询时正确处理）
   - 角色A和角色B的回复都正确保存，且 `appId` 不同

## 下一步行动

1. 添加详细的日志输出，追踪消息保存和查询的完整流程
2. 运行测试，观察日志输出
3. 根据日志结果，确定具体的问题点
4. 实施相应的解决方案

