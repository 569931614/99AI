# 群聊角色混淆问题修复说明

## 问题描述

在群聊模式下，当用户问"你是谁？"时，AI 角色（秦彻、祁煜）会回答成其他角色（如 5699）的身份信息，出现角色混淆。

### 问题日志示例

```
[Nest] 48636  - 2025/10/04 17:11:49   DEBUG [ChatService] 添加当前用户提问到消息历史: 5699：你是谁？ 
[Nest] 48636  - 2025/10/04 17:11:52   DEBUG [OpenAIChatService] 群聊响应数据: {...,"content":"5699，性别男，身高186cm，体重73kg..."}
```

## 问题根源分析

### 1. 历史消息缺少 appId 字段

**位置**: `service/src/modules/chatLog/chatLog.service.ts` 第 301-330 行

**问题**: `chatHistory` 方法返回的历史消息记录中没有包含 `appId` 和 `createdAt` 字段，导致：
- 无法识别历史消息是哪个 AI 角色发送的
- 日志显示 `appId=undefined`
- 无法正确区分当前角色和其他角色的消息

### 2. System Prompt 不够强化

**位置**: `service/src/modules/aiTool/chat/chat.service.ts` 第 928-974 行

**问题**: 
- 群聊模式下的 system prompt 没有明确强调当前角色的身份
- 当历史消息中包含其他角色的自我介绍时，AI 容易混淆身份
- Prefill 机制虽然正确，但不足以防止身份混淆

### 3. 其他角色回复缺少说话人标识

**位置**: `service/src/modules/chat/chat.service.ts` 第 1109-1130 行

**问题**:
- 其他角色的历史回复被转换为 `user` 角色消息时，可能缺少说话人标识
- 导致当前角色无法区分这是其他角色的发言

## 修复方案

### 修复 1: 在历史消息中添加 appId 和 createdAt 字段

**文件**: `service/src/modules/chatLog/chatLog.service.ts`

**修改内容**:
```typescript
const record = {
  role: role,
  content: content || (role === 'assistant' ? answer : prompt),
  imageUrl: imageUrl || fileInfo || '',
  fileUrl: fileUrl,
  ttsUrl: ttsUrl,
  videoUrl: videoUrl,
  audioUrl: audioUrl,
  reasoningText: reasoning_content,
  tool_calls: tool_calls,
  progress,
  appId: appId,  // ✅ 添加 appId 字段，用于群聊模式识别角色
  createdAt: createdAt,  // ✅ 添加 createdAt 字段，用于消息排序
};
```

**效果**:
- 历史消息现在包含 `appId`，可以准确识别是哪个角色的回复
- 包含 `createdAt`，确保消息按时间正确排序

### 修复 2: 强化 System Prompt，明确角色身份

**文件**: `service/src/modules/aiTool/chat/chat.service.ts`

**修改内容**:
```typescript
// 从 normalizedMessages 中提取 system prompt（当前角色的人设）
const systemMessage = normalizedMessages.find(m => m.role === 'system');
if (systemMessage) {
  currentRoleSystemPrompt = systemMessage.content;
}

// 强化 system prompt，明确当前角色身份
let enhancedSystemPrompt = currentRoleSystemPrompt;
if (currentRoleSystemPrompt) {
  enhancedSystemPrompt = `你是 ${currentRoleName}。以下是你的完整人设：\n\n${currentRoleSystemPrompt}\n\n重要提醒：\n1. 你只能以 ${currentRoleName} 的身份回答问题\n2. 不要混淆其他角色的身份信息\n3. 当被问到"你是谁"时，只回答 ${currentRoleName} 的身份\n4. 严格遵守你的人设，不要扮演其他角色`;
}

// 构建消息列表：替换 system 消息为强化版本
const messagesForGroupChat = normalizedMessages
  .filter(m => m.role !== 'system')
  .map(m => ({ ...m }));

// 在开头添加强化的 system prompt
if (enhancedSystemPrompt) {
  messagesForGroupChat.unshift({
    role: 'system',
    content: enhancedSystemPrompt
  });
}
```

**效果**:
- 明确告诉 AI 当前扮演的角色名称
- 强调不要混淆其他角色的身份
- 特别针对"你是谁"这类问题给出明确指引

### 修复 3: 为其他角色的回复添加说话人标识

**文件**: `service/src/modules/chat/chat.service.ts`

**修改内容**:
```typescript
let messageContent = msg.content;

// 如果是其他角色的回复，需要确保内容已经包含说话人标识
// 如果没有，则添加说话人标识以避免身份混淆
if (!isCurrentMember && typeof messageContent === 'string') {
  // 检查消息是否已经有说话人前缀（格式：角色名：内容）
  const hasSpeakerPrefix = /^[^：]+：/.test(messageContent);
  
  if (!hasSpeakerPrefix) {
    // 尝试从 groupMembers 中找到该角色的名称
    const msgMember = groupMembers.find(m => (m.appId || m.userId) === msgAppId);
    const speakerName = msgMember ? (msgMember.appName || msgMember.name || `角色${msgAppId}`) : `角色${msgAppId}`;
    
    // 添加说话人前缀，明确这是其他角色的发言
    messageContent = `${speakerName}：${messageContent}`;
    Logger.debug(`[群聊历史] 为其他角色消息添加说话人标识: ${speakerName}`, 'ChatService');
  }
}

messages.push({
  role: finalRole,
  content: messageContent
});
```

**效果**:
- 确保其他角色的回复都带有明确的说话人标识
- 当前角色看到历史消息时，能清楚区分是谁在说话
- 避免将其他角色的自我介绍误认为是自己的身份

## 测试建议

### 测试场景 1: 基本身份识别
1. 在群聊中向秦彻提问："你是谁？"
2. 预期回答：秦彻的身份信息（姓名、年龄、职业等）
3. 在群聊中向祁煜提问："你是谁？"
4. 预期回答：祁煜的身份信息

### 测试场景 2: 多轮对话后的身份识别
1. 先让 5699 回答"你是谁？"
2. 再让秦彻回答"你是谁？"
3. 预期：秦彻不会回答成 5699 的身份

### 测试场景 3: 历史消息中的身份区分
1. 在群聊中进行多轮对话，包含多个角色
2. 每个角色都被问"你是谁？"
3. 预期：每个角色都能正确回答自己的身份，不会混淆

### 测试场景 4: 检查日志
查看日志中的以下关键信息：
- `[群聊历史] AI消息 (appId=X, 当前=Y, role=...)` - appId 应该有值，不再是 undefined
- `[Prefill] 找到角色名称: XXX` - 应该显示正确的角色名称
- 消息内容应该包含说话人前缀（如 "秦彻：..."）

## 部署步骤

1. 停止当前运行的服务
   ```bash
   cd service
   pnpm stop
   ```

2. 重新编译服务
   ```bash
   pnpm build
   ```

3. 启动服务
   ```bash
   pnpm start
   ```

4. 观察日志，确认修复生效
   ```bash
   # 查看 PM2 日志
   pm2 logs
   ```

## 预期效果

修复后，群聊模式下：
1. ✅ 每个 AI 角色都能正确识别自己的身份
2. ✅ 不会混淆其他角色的身份信息
3. ✅ 历史消息中的 appId 正确传递
4. ✅ 说话人标识清晰明确
5. ✅ System prompt 强化了角色身份意识

## 相关文件

- `service/src/modules/chatLog/chatLog.service.ts` - 历史消息查询
- `service/src/modules/aiTool/chat/chat.service.ts` - 星尘 API 调用和 prefill 机制
- `service/src/modules/chat/chat.service.ts` - 消息历史构建和说话人标识

## 技术细节

### 星尘群聊 API 规范
参考文档：https://help.aliyun.com/document_detail/2861866.html

关键要点：
1. 每个 message 需要在 content 的开始标识说话人名（例如："程毅：周末你们有空不？"）
2. 其他聊天对象的 role 为 user，角色的 role 为 assistant
3. 调用时，需要以当前角色名作为 prefill 输入（最后一条 assistant 消息只包含角色名）

### Prefill 机制
Prefill 是一种引导 AI 回复的技术，通过在最后添加一条 assistant 消息（只包含角色名），让 AI 以该角色的身份继续回复。

示例：
```json
{
  "messages": [
    {"role": "system", "content": "你是秦彻..."},
    {"role": "user", "content": "5699：你是谁？"},
    {"role": "assistant", "content": "秦彻："}  // Prefill
  ]
}
```

AI 会自动补全 "秦彻：" 后面的内容，确保以秦彻的身份回答。

